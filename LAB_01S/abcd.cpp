#include <iostream>
using namespace std;

int main(){
    int sum=0, temp;
    cin >> temp;
    sum+=temp;
    cin >> temp;
    sum+=temp;
    cin >> temp;
    sum+=temp;
    cin >> temp;
    sum+=temp;
    cout << sum << endl;
}